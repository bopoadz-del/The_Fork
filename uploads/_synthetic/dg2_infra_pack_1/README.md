# DG2 Infra Pack 1 — selected documents

Pulled from Google Drive `Master Folder / DG2 Infra Pack 1` into the live
project `f1c78383` (Infra Pack UI Aug 23), then saved here so they land on
the Windows checkout at:

`C:\Users\shimm\The_Fork\uploads\_synthetic\dg2_infra_pack_1`

These are **not** the leftover fixtures (`leftover_mini_boq.xlsx`,
`khor_waterproofing_spec.docx`, `trench_site.jpg`, `resource_loaded.xer`).

See `MANIFEST.json` for Drive/project document ids and byte sizes.
See `../dg2_pack_17_questions.json` for the new 17-action prompts.
