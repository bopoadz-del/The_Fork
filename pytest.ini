[pytest]
asyncio_mode = auto

# Fail on unknown markers and on a malformed config — a typo
# (e.g. @pytest.mark.asyncoi) becomes an error, not a silent no-op.
addopts = --strict-markers --strict-config
